ARG FROM_IMAGE_NAME=jfrog.tecorigin.net/tecotp-docker/release/ubuntu22.04/x86_64/pytorch:1.0.0-torch_sdaa1.3.0
FROM ${FROM_IMAGE_NAME}

WORKDIR /workspace/
ADD requirements.txt /workspace/
RUN rm -rf /bin/sh && ln -s /bin/bash /bin/sh
RUN source activate torch_env && pip install --no-cache-dir -r requirements.txt
RUN sudo apt-get install libxml-parser-perl
ADD . /workspace/contrib/Pose/MRT/
WORKDIR /workspace/contrib/Pose/MRT/
