- case_name:
  desc:
    run_dir:
    run_file:
  params:
    nnodes:
    nproc_per_node:
    batch_size:
    model_name:
    dataset_path:
    step:
    epoch:
    lr:
    device:
    autocast:
    others: #没有自定义参数删除该字段即可
